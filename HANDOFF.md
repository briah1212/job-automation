# Handoff Document

This document exists so that anyone, human or AI agent, can pick up this project without needing access to any server, prior chat history, or tribal knowledge.
It was requested at the end of a previous AI coding session but was never actually written until now.
Read this first, then follow the pointers at the bottom for deeper detail.

## Product Overview

`job-automation` is a single-user AI-powered platform for job discovery, resume customization, application submission, and application tracking.
The full product specification lives in `spec.md` at the repo root.

Core principles, per the spec, that any future work must respect:

- Truthfulness: never fabricate resume facts.
- Human-in-the-loop control: the system supports Manual, Assisted, and Trusted-Autopilot modes.
  Assisted is the default mode.
  Autopilot must stay off by default.
- No CAPTCHA or anti-bot circumvention of any kind.
- Quality over volume in job matching and applications.
- Explainability: matching and tailoring decisions should be traceable and understandable, not a black box.
- Idempotency: operations should be safe to retry without duplicating side effects.

## Architecture and Tech Stack

**Backend**: Python 3.9, FastAPI, SQLAlchemy, Alembic, Pydantic v2.
JWT authentication is implemented with direct calls to the `bcrypt` library rather than through `passlib`, because `passlib`'s `CryptContext` had compatibility problems with newer bcrypt releases (see Known Issues below).

**Frontend**: Next.js 14 using the App Router, TypeScript, Tailwind CSS, shadcn/ui components, and NextAuth.js for session handling.

**Database**: PostgreSQL 15, using UUID primary keys and a mix of JSONB and native Postgres `ARRAY` columns.

**Cache**: Redis 7.

**Object storage**: MinIO, an S3-compatible store.

**Browser automation**: Playwright (Python), driven through an ATS-adapter abstraction.
There is a base adapter interface, a working mock-ATS adapter, and a generic stub adapter intended as the starting point for future Greenhouse, Lever, Ashby, and Workday adapters.

**AI layer**: a provider-agnostic "AI Gateway" that supports `mock`, `anthropic`, and `openai` backends, selected via the `AI_PROVIDER` environment variable.
`mock` is the default and is the only provider that has actually been exercised so far.
No real LLM calls have been verified end-to-end yet.

**Workflow orchestration**: a DB-backed `workflow_tasks` table currently stands in for Temporal, which is what the spec originally recommended.
This was deliberately designed so that a future migration to real Temporal would not require rewriting business logic, only the orchestration layer around it.

## What's Done

### Phase 1 - Foundation and Minimal Vertical Slice

- Auth, profile CRUD, resume family/version models.
- Job import and normalization.
- Application records.
- The `workflow_tasks`, `audit_events`, and `model_calls` (AI cost tracking) tables.
- A Next.js frontend scaffold covering dashboard, auth, profile, resumes, jobs, and applications pages.
- A Playwright browser-worker with a mock-ATS adapter and checkpoint/resume support, plus a 3-page mock ATS fixture site used for testing.
- The backend was verified end-to-end via `curl` during that session.
- The frontend was built but was not actually deployed or verified until the current session (see the placeholder status section below for this session's findings).

### Phase 2 - Matching and Resume Selection

- `SearchProfile` model: career categories, include/exclude titles and skills, locations, remote policy, minimum salary, employment types, seniority levels, and target/excluded companies.
- `JobMatchScore` model: an overall score plus per-dimension scores (0-100 integers), hard blockers, strong matches, soft gaps, missing info, a recommended action, and a human-readable explanation.
- `MatchingAgent`: a weighted scorer using skill match (35%), experience (25%), seniority (15%), location (15%), and salary (10%), plus separate hard-blocker checks that can override the weighted score.
- `ResumeSelectionAgent`: scores candidate resume versions using coverage (40%), relevance (30%), recency (20%), and historical performance (10%).
- New API routes: full CRUD for search profiles, plus job match and select-resume endpoints.
- New frontend: search-profiles pages, an enhanced job inbox with filtering, and `MatchAnalysis` and `ResumeRecommendation` components.
- 18/18 backend tests passing, and a full API integration test passing.
- Committed as `335bb18` and pushed to GitHub.

### Phase 3 - Tailoring and Review (complete, 2026-07-17)

- Data model: `ProfileFact` (granular, provenance-tracked facts extracted from resumes/profile), `ResumeClaim`/`ResumeClaimSource` (per-bullet provenance linking generated resume text back to facts), `DocumentRendering`/`DocumentLock` (rendered PDF artifacts and user-lockable fields), `ApplicationQuestion`/`ApplicationAnswer`/`ApplicationAnswerSource` (normalized Q&A, replacing the old JSONB blob), `ReusableAnswer` (approved-answer knowledge base), `ApplicationReview` (automated review results).
- `ResumeTailoringAgent`: builds a requirement-evidence matrix (job requirements matched against verified profile facts, with strength scores and fact text shown), calls the AI gateway to draft a tailored resume variant, enforces user-set locks (exact title/dates/protected accomplishments) by reverting any locked field the AI tries to change, and records per-claim provenance.
- Resume rendering (PDF via reportlab), a diff viewer (added/removed/reordered content, keyword and summary changes, warnings), and field locks - all exposed via `POST /api/resumes/{id}/tailor`, `POST /api/resumes/{id}/render`, `GET /api/resumes/{id}/diff`, `POST/GET /api/resumes/families/{id}/locks`.
- `ApplicationQuestionAgent`: implements the spec's strict answer precedence (exact/canonical reusable answer -> deterministic calculation -> AI-generated from verified facts only -> require user input), a deterministic years-of-experience calculator (never guesses if dates can't be parsed), and hard-blocks guessing on high-risk questions (work authorization, sponsorship, etc.).
- `ApplicationReviewAgent`: an 18-point-style checklist (missing resume, unanswered high-risk questions, placeholder text, malformed dates, duplicate applications, wrong-company heuristics) plus one AI call using a deliberately separate prompt from the tailoring agent, per the spec's independence requirement.
- Frontend: real resume detail page (requirement-evidence matrix, diff viewer, lock management), real application review workspace (question generation, inline answer editing with source labels, review findings with pass/fail and confidence), and the resumes/applications list pages now show real data instead of Phase 1's hardcoded mocks.
- Also fixed while verifying this phase: the entire browser-based auth flow was non-functional (wrong login request shape/target, missing CORS origin, no JWT ever attached to API calls, no dashboard route guard, no `SessionProvider`) - all repaired and verified via both curl and a real browser walkthrough.

### Job Extraction Worker and Cover Letter Generation (complete, 2026-07-17)

Built at the user's direction, ahead of the spec's own phase order, because several Phase 3 features had nothing real to operate on without them:

- **Job extraction worker**: a new standalone process (`backend/worker.py`, run as its own `job-worker` Docker service, not gated behind a profile - it starts automatically with `docker compose up -d`) polls the `workflow_tasks` table for pending `job_extraction` tasks. `POST /api/jobs/import-url` now creates one of these tasks instead of leaving the job stuck in `extracting` forever. The worker fetches the job URL's raw HTML, strips it to plain text, runs a new `ExtractionAgent` (populates company/title/location/salary/skills/requirements) and a new `ClassificationAgent` (assigns a career category), and writes the result into `CanonicalJob.extracted_data` using the exact keys (`skills`, `category`, `seniority_level`, `requirements`) that `MatchingAgent` and `ResumeSelectionAgent` were already reading but never actually receiving real data for. Seniority is inferred heuristically in Python (title keywords, falling back to years-of-experience bands) rather than via an AI call, to keep this workstream independent of the AI gateway schema files. Verified end-to-end live: importing the mock-ats URL produces a fully populated, realistic `extracted_data` within seconds.
- Fixed a latent bug found while building this: `WorkflowTask`'s response schema declared a field named `metadata`, but the model column is `task_metadata` - same class of name-mismatch bug documented elsewhere in this file. Fixed to match.
- **Cover letter generation**: new `CoverLetter` model/table, `CoverLetterAgent` (same truthfulness discipline as the resume tailoring agent - only verified facts, must reference the specific role and company, avoid generic enthusiasm, connect 2-3 real experiences to the job's stated needs, respect tone and word-limit preferences), and three endpoints (`POST/GET/PATCH /api/applications/{id}/cover-letter`). Every generated letter starts as `needs_review`; saving an edit through the UI marks it `approved` (this is the review gate - there's no separate reusable-approval-policy system, kept intentionally simple per the user's request not to over-build this). Frontend: a self-contained `CoverLetterCard` component on the application review workspace with a tone selector, word-limit input, generate/regenerate, live word count, and an editable, savable textarea.
- Resume selection/tailoring itself needed no code changes - `ResumeSelectionAgent` already selected resumes by matching `ResumeFamily.target_category` against `job.extracted_data["category"]`, and `ResumeTailoringAgent`'s prompt already framed its job as reordering/emphasizing existing verified content rather than generating from scratch. Both were sitting unexercised for lack of real extracted job data - the worker above is what makes them actually useful now.

## What's Next

The following phases are defined in `spec.md` section 27 but have not been started.
Consult that section for full detail before starting any of them.

- **Phase 4 - Browser-Assisted Applications (partially built, 2026-07-20)**: `browser-worker` is now a real persistent Docker service, not just an on-demand script.
  It runs `browser_worker/queue_worker.py`, a poll loop (mirroring `backend/worker.py`'s pattern) that claims pending `WorkflowTask` rows with `workflow_type="browser_submission"` - the ones `backend/app/api/routes/browser_automation.py` creates - and drives them through `BrowserWorker.process_application`/`resume_from_checkpoint`.
  Verified end-to-end live against the mock-ats fixture: fill pages 1-3, checkpoint, pause in assisted mode (`waiting_user_input` / `pipeline_status=paused`), `approve-submit` resumes and replays the checkpointed field values across all pages (see bug fix #14 below - the mock ATS is a client-side single-URL multi-page form, so a fresh page load always resets to page 1), then attempts final submit.
  Still missing, and blocking a full pass through to `confirmed`: field mapping and learning (the generic ATS adapter beyond the mock, and mapping richer canonical fields like consent/attestation checkboxes - see Known Remaining Gaps), resume upload through the browser (the API's resume upload endpoint doesn't persist the file yet - also see Known Remaining Gaps), and a manual takeover UI / submission preview on the frontend (the backend API for approve/cancel/answer-question exists; no frontend UI calls it yet).
  - **Browser state machine work (2026-07-20)**: full design at `docs/browser-state-machine-design.md`, reviewed and approved before implementation started, per explicit instruction.
    Phases 1-2 of that design are built and independently verified live: a durability layer (`browser_sessions`/`browser_checkpoints`/`ats_credentials` in Postgres, screenshots in MinIO - the first thing in this codebase to actually use MinIO, which had been running unused since Phase 1) replacing the old container-local `/tmp/checkpoints` files, plus an orphan-reclaim query for `WorkflowTask`s stuck in `running` after a crash; and an encrypted ATS-credential vault (`CredentialCipher` interface, `FernetCredentialCipher`, a new `INTERNAL_API_KEY`-gated `/api/internal/ats-credentials/*` route) for the auto-account-creation capability planned in Phase 3-4 of that design.
    Verified: the encryption key never reaches the `browser-worker` container's environment (checked directly, not just by reading the compose file) - only `api`/`job-worker` have it, matching the design's threat model (browser-worker is the process exposed to untrusted page content).
    An independent review pass (a fresh subagent, not just self-review) caught and fixed six real issues, most notably `form_state` checkpoints reaching Postgres unredacted - see Known Issues below.
    A second independent live-testing pass (another fresh subagent, full read/write, told to fix anything it found) re-verified Phases 1-2 end to end - including a container restart mid-flow - and found nothing further wrong.
    **Phase 3 (2026-07-20) is also done and verified live**: `BrowserState` (`browser_worker/state.py`) and `ATSAdapter` v2 (`detect_state`/`handle_state`) replaced `_fill_application`'s linear loop with a real state machine (`BrowserWorker.run`/`resume`/`run_state_machine`) driving `landing -> apply -> login`/`create_account -> profile_setup -> resume_upload -> resume_parse_wait -> application -> review -> submit_ready -> submitted`, with `manual_intervention` escalation (CAPTCHA/MFA/email-verification/unsupported-flow/repeated-failure) and the pre-existing assisted-mode approval gate at `submit_ready` both intact.
    The mock-ats fixture was extended with the whole pre-application flow (`fixtures/ats-sites/mock-ats/`) specifically so this could be verified against every state, not just the three pages that existed before.
    Verified live: a complete run through every single state to a real `submitted` confirmation; the `email_verification` and missing-resume `manual_intervention` paths; `GenericAdapter`'s multi-signal `detect_state` independently classifying all 12 states correctly using zero fixture-specific selectors; and credential-vault reuse actually working end-to-end (a second application for the same user took the `LOGIN` branch and authenticated successfully against a previously auto-created account).
    That last check caught a real fixture design flaw along the way: the original fixture drafted account state into `localStorage`, which Playwright's `browser.new_context()` isolates per run, so it could never actually prove login-reuse worked - fixed by moving account storage into `server.py` (a real, if in-memory, server-side store), which is what actually let credential reuse be verified as opposed to just asserted.
    Also caught live: `resume()` navigating to the bare `application_url` reset the hash-routed SPA back to `landing`, discarding all client-side progress - fixed by navigating to the checkpoint's own stored URL instead, exactly like the original `resume_from_checkpoint` did before this session's refactor (a detail that got lost in the rewrite and only surfaced through an actual full-approval-cycle test, not by reading the diff).
    Not yet built (all closed in the 2026-07-21 session below): the dynamic-questions bridge, resuming a `manual_intervention`-paused task, a committed test suite, and real Workday validation (still open).
  - **Manual intervention, dynamic questions, field-learning, and automated testing (2026-07-21) - all four built and verified live.**
    - **Manual intervention resume**: `POST /api/applications/{id}/resume-manual-intervention` (new, `browser_automation.py`) flips a `waiting_user_input`/`manual_intervention` task back to `pending`. Made generic on purpose: `_pause_for_manual_intervention` in `worker.py` was simplified to *not* write its own synthetic checkpoint, so the resume anchor is always whatever real `BrowserState` checkpoint was last written - `resume()`'s existing structural-vs-replay logic then just works for any pause reason without new branching. Verified live by pausing on a missing resume, attaching one externally, and resuming without restarting the run.
    - **Dynamic questions**: new `POST /api/internal/dynamic-questions/answer` calls `ApplicationQuestionAgent` (reusing its existing precedence: exact reusable answer -> no-guess on high-risk -> deterministic calculation -> AI-generated from verified facts -> ask the user) with profile facts/reusable answers fetched fresh per call. `browser_worker/services/field_resolution.py` (new, shared by both adapters to kill duplication) calls it whenever a *required* field has no FieldMapper match, and introduces `_constrain_to_options` to map a free-text answer onto a `<select>`'s actual option values (an AI-generated "Yes, happy to relocate" can't be handed to `select_option()` directly) - falls back to pausing with `paused_question` rather than guessing when no confident match exists. `answer-pending-question` now also writes a `ReusableAnswer` (`user_approved=True`), so an answered question is reused automatically on every future application without hitting the agent again - verified live (second run's identical question never re-paused). The LLM still has zero path to submission authority - it only ever fills one field, `submit_ready`'s human-approval gate is untouched.
    - **Persistent field-mapping learning**: new `field_mappings` table (migration `401ceec878a9`, model `FieldMapping`), scoped by `(ats_platform, domain, form_fingerprint, field_name)` per spec 14.3, deliberately *not* user-scoped (a site's form shape is the same for everyone). `field_resolution.py` checks it before FieldMapper's regex, and records a mapping the first time regex succeeds - verified live across three separate application runs: `use_count` correctly went 1 -> stayed 1 (cache hit, initially a real bug - see Known Issues #18) -> 2. Reviewable via `GET/PATCH/DELETE /api/field-mappings` (new, JWT-authed, not internal) - all three verified live.
    - **Automated tests**: `apps/browser-worker/tests/` gained `test_field_resolution.py` and `test_checkpoint_manager.py` (fast, pure unit tests, no browser/network), and the stale pre-refactor `test_adapters.py` (referenced `process_application`/`resume_from_checkpoint`, both removed in the Phase 3 rewrite) was replaced with `test_mock_ats_adapter.py` and `test_generic_adapter_detect_state.py` matching the current fixture. New top-level `tests/integration/` (host-driven, against the real dockerized stack's published ports, `docker compose up -d` then `pytest tests/integration`) covers full submission, manual-intervention resume, dynamic questions, field-learning reuse, credential reuse, and - the one that actually matters most for this durability work - a real `docker compose restart browser-worker` mid-pause, asserting the task survives and stays resumable. All 8 integration tests pass. New `.github/workflows/browser-worker-tests.yml` runs both suites in CI.
    - ~~**Known test-suite limitation, found and not resolved**~~ - **resolved 2026-07-21, root cause found**: not actually about running the two files *together* - reproduced identically by running `test_generic_adapter_detect_state.py` alone, twice in a row, with no other test file involved. `fixtures/ats-sites/mock-ats/server.py`'s account store (`_ACCOUNTS = {}`) is a real, process-lifetime dict on a long-lived `docker-compose` service that is never restarted between separate `pytest` invocations (confirmed: the container had 11 hours of uptime spanning many earlier test runs this session). The "unique" emails weren't: `test_generic_adapter_detect_state.py`'s `itertools.count()`-based counter resets to 0 every fresh pytest process, so `genprofile-0@example.com` etc. were byte-identical across every run against the same server, and `test_mock_ats_adapter.py` used flatly fixed emails (`inspecttest@example.com`, `navtest@example.com`). Once any prior run had registered one of these, every later run's signup 409-collided, silently stayed on the signup stage instead of navigating to `profile-setup`, and every downstream `select_option`/fill against a field on a stage that was never actually displayed timed out three-plus steps later - a `run_id = uuid.uuid4().hex[:12]` component was added to every test email in both files so uniqueness holds across process restarts, not just within one. Verified: three consecutive full-suite runs (`pytest tests/`) against the same long-lived server, all 57 tests passing every time. The earlier ruled-out hypotheses (email collisions "within" a run, the `browser` fixture name, timing margins) were all real dead ends on their own terms - the actual bug was uniqueness *across* runs, a dimension none of them tested.
  - Real bugs found and fixed *during* this work (beyond the field_mappings cache-hit tracking gap already mentioned): a migration bug where `sa.Column(index=True)` plus an explicit `op.create_index()` for the same column raced/duplicated (fixed by dropping the redundant explicit calls); a stale-metadata cosmetic bug where a prior pause's `detail`/`pause_reason` lingered in `task_metadata` after a completely different outcome (fixed by clearing known transient keys before each run's outcome is written); and the `_constrain_to_options` substring matcher not normalizing underscores/dashes against natural-language answers (e.g. "job board" vs. `job_board`), caught by its own unit test.
  - Still not built: real Workday validation (design doc section 11, item 7 - deliberately last, needs an actual Workday deployment), and the CI workflow has not been run on a real GitHub Actions runner (only the equivalent commands, run by hand against the same dockerized stack this whole session used).
  - **Real-ATS validation against Workday, Greenhouse, Lever, and Ashby (2026-07-21) - researched all four, hardened the generic architecture, zero platform-specific adapters created.**
    Methodology: `WebSearch` for a live, real job posting on each platform, then headless Playwright navigation (view-only - no form submission, no real personal data entry, no account creation) dumping headings/buttons/every form field's name/id/type/geometry/aria attributes at each stage of the real apply flow, cross-referenced against `BrowserState`/`GenericAdapter`. Every finding turned out to be generalizable; **no `WorkdayAdapter`/`GreenhouseAdapter`/etc. was built**, deliberately, per the explicit instruction not to introduce platform-specific hacks unless the generic architecture genuinely couldn't cover it - it always could.
    - **Workday** (real NVIDIA posting): confirmed the existing `landing -> apply -> login/create_account -> ... -> application -> review -> submit_ready` sequence matches Workday's real flow, including a persistent stepper/breadcrumb and a client-side (no real navigation) transition on Apply. Found a honeypot field (`data-automation-id="beecatcher"`, `name="website"`) that is `display:block; visibility:visible; opacity:1` with a `1px x 0.01px` box - it passes Playwright's `:visible` pseudo-class cleanly while being unusable by a human, meaning `GenericAdapter` would have blindly filled it with fabricated data on a real submit, risking the whole session getting flagged as a bot. This was a real, live, previously-undetected gap - not hypothetical.
    - **Greenhouse** (real MNTN posting): single-page application form with **no login/account step at all** (structurally different from Workday - confirms the state machine must tolerate a `LOGIN`/`CREATE_ACCOUNT` skip, which it already does by design since those states are only entered if `_handle_apply` actually finds an auth choice). A real reCAPTCHA widget is present. EEO/demographic fields (gender, ethnicity, veteran status, disability status) are rendered as near-invisible custom-styled widgets - superficially similar geometry to the Workday honeypot but for the opposite reason (a real, required, legally-protected field wrapped by a custom UI, not a bot trap).
    - **Lever** (real Palantir posting): also a single-page form, no login. A cookie-consent banner ("Deny"/"Accept") blocks the page before Apply is reachable. Uses **hCaptcha**, not reCAPTCHA - confirms CAPTCHA handling must be vendor-agnostic. Custom application questions use opaque UUID field names (`cards[<uuid>][field0]`), matchable only by their rendered label, never by name/id. EEO follow-up fields (`eeo[disabilitySignature]`) are legitimately `0x0` until a prior question is answered - progressive disclosure, not a honeypot and not permanently hidden.
    - **Ashby** (real Vanta posting): single-page form, no login, reCAPTCHA present again. EEO fields carry a reliable naming convention (`..._systemfield_eeoc_gender`, `_eeoc_race`, `_eeoc_veteran_status`), and canonical fields use a `_systemfield_` prefix. File uploads are a 1x1 native input behind a styled "Upload file" button.
    - **Hardening implemented as a result** (all in the generic layer, used by both `GenericAdapter` and `MockATSAdapter`, none of it ATS-specific):
      - `browser_worker/services/field_visibility.py` (new): `is_genuinely_fillable()` - the honeypot-vs-legitimate-widget discriminator. A pure size threshold can't tell Workday's beecatcher apart from Ashby's 1x1 file input or Greenhouse's near-invisible EEO `<select>`, since real ATS platforms routinely ship near-zero-geometry *legitimate* fields. The signal that actually separates them: a honeypot exists specifically to have no human-discoverable label; everything else in that list still has one (an associated `<label>`, `aria-label`, or wrapping text) even though the native control itself is visually suppressed. `type=file` is exempted categorically (universal custom-upload-button pattern); CAPTCHA response field names (`recaptcha`/`hcaptcha`/`turnstile`/`captcha`) are excluded categorically (populated by vendor JS, never applicant data). Wired into both adapters' `inspect_form`.
      - `browser_worker/services/captcha_detection.py` (new): `detect_captcha_challenge()` - vendor-agnostic (reCAPTCHA v2, hCaptcha, Cloudflare Turnstile), and deliberately triggered only by the *visible interactive widget container/iframe*, not by mere presence of the hidden response field. Invisible reCAPTCHA v3 injects that field on nearly every page load and scores traffic silently in the background without ever presenting a challenge - a naive "field present" check would have paused on nearly every Greenhouse/Ashby application regardless of whether a human was ever actually needed. Wired into `worker.py`'s main loop, checked every transition ahead of `adapter.detect_state` (so it fires regardless of which state the page would otherwise classify as), escalating via the already-defined-but-previously-unused `PauseReason.CAPTCHA`. Spec-mandated ("no CAPTCHA or anti-bot circumvention of any kind") - never attempts to solve or interact with a challenge, only recognizes one is present.
      - `browser_worker/services/cookie_consent.py` (new): `dismiss_cookie_consent()` - clicks the most privacy-preserving option on a visible banner (reject/decline preferred, accept only as a fallback when no reject option exists). Called once, immediately after each fresh navigation in both `run()` and `resume()` - deliberately not on every state-machine tick, since the word lists (especially bare "deny"/"reject", the real Lever button text) are broad enough that repeated matching risks misfiring on an unrelated control later in the flow (e.g. a "Reject changes" button on a form page).
      - **EEO/demographic risk classification** (`backend/app/api/routes/application_questions.py`): `_HIGH_RISK_KEYWORDS` had no coverage at all for race/ethnicity/gender/veteran/disability terms - a real question text like "Veteran Status" would have fallen through to the default "medium" tier despite the spec's hard "never guess high-risk" rule already existing in `ApplicationQuestionAgent`. Fixed by adding the exact terms confirmed live across all three platforms, plus a new `demographic_self_id` question type. New test file `backend/tests/test_application_questions_classification.py`.
    - **Deliberately not built**: new `BrowserState` values for Workday's "My Experience"/"Voluntary Disclosures" stepper steps, or for its "Start Your Application" (Autofill/Manual/Use Last Application) and OAuth-vs-email login choice screens. Both are already covered by the existing generic `APPLICATION` state (any form page with fields to fill, advance when done) and by `_handle_apply`'s button-word search - adding platform-shaped states would have been exactly the kind of unnecessary platform-specific modeling the task explicitly said to avoid, and neither was needed for anything downstream (EEO/high-risk handling is routed by field content, not by which page state it's found on).
    - **Flaky-suite root cause found and fixed, not masked** (previously an open item - see the old Known Issues #114/174 entries, now resolved below).
    New/updated tests: `test_field_visibility.py`, `test_captcha_detection.py`, `test_cookie_consent.py` (all new, isolated `page.set_content` unit tests), `test_application_questions_classification.py` (new, backend), plus two new fields added to the mock-ats fixture's page 1 (a honeypot and a legitimate near-zero labeled field) with matching assertions in `test_mock_ats_adapter.py`. Verified live end-to-end through the full state machine (`worker.run()` against `mock-ats`, real credential-vault signup, real field-mapping/dynamic-question calls) with all of the above wired in together - the honeypot was silently skipped, the legitimate near-zero field was correctly filled/considered, and nothing regressed. 57/57 `apps/browser-worker/tests` pass, 90/90 backend tests pass.
- **Real-ATS validation, live runs with real credentials (2026-07-21/22) - built a replay/debug system, then ran real applications against Workday, Greenhouse, Lever, and Ashby using the applicant's own real resume/contact info, found and fixed 14 real bugs.**
  Per explicit instruction, this replaced the earlier session's view-only research pass with actual live runs: a real test account, a real `Profile` (the user's own name/phone/email/linkedin from `sample_resume.pdf`, gitignored - never committed), and the real product pipeline (`/api/resumes` upload, `CanonicalJob`/`Application` rows, `/api/applications/{id}/start-browser`) - not a bypass script.
  Every run stopped at `submit_ready`/`awaiting_approval` or a genuine pause; **zero real applications were ever actually submitted** to a real employer, matching the spec's assisted-mode approval gate.
  - **Replay/debug system built first** (a prerequisite named explicitly in the instruction, not built before this): `browser_checkpoints` gained `dom_snapshot_object_key`, `decision_reasoning`, `field_sources`, `action_log` columns (migration `0b2d243a00d8`); `GenericAdapter`/`MockATSAdapter` now record *why* each state was detected (per-candidate-state scores + raw signals); `field_resolution.py` records *where* each filled value came from; `RunContext.log_action` records every action taken. `backend/app/services/replay_report.py` (new) assembles all of it into one self-contained HTML timeline per session (`GET /api/applications/{id}/replay`) - screenshots and DOM snapshots embedded as base64. This is what actually made every bug below diagnosable from stored data instead of live reproduction.
  - **Found and fixed while root-causing real pauses/failures**, most confirmed independently against the real live page, not just inferred from logs:
    1. `POST /api/resumes` never actually persisted the uploaded file (a literal `# TODO`) - blocked real validation outright, since `browser-worker` resolves the same `file_path` against a shared storage volume. Fixed: writes bytes to `_STORAGE_ROOT`, filename sanitized.
    2. `_score_landing`/`_score_review` matched button text via exact list membership instead of substring, unlike every other button check in the file - a real "Apply for this job"/"Submit Your Application Now" button scored too low to ever be clicked.
    3. **Critical**: `_score_submit_ready` gave 0.5 confidence to a completely unrendered Ashby page (still showing "you need to enable javascript to run this app", zero visible fields) purely because a static nav link contained the word "apply" - reported `awaiting_approval` with nothing ever filled in. Hardened twice: requires `visible_field_count > 0`, then `has_any_filled_field` (a real non-empty value, or a checked consent checkbox) - field *presence* isn't evidence of being filled.
    4. `_handle_landing` clicked Apply and unconditionally reported success with no check anything happened - against a real Workday posting, the click was a genuine no-op (no navigation, no content change, even after several seconds; a direct `/apply`-suffix navigation also never reached `networkidle` within 30s - looks like server-side bot detection, unconfirmed) and the state machine burned all 40 transitions before giving up with a vague error. Now verifies the page actually changed and fails fast with a specific one.
    5. `inspect_form`'s label fallback only checked `label[for=id]`; Lever's "Cards" custom-question widget associates a label with its field purely through DOM sibling position - a real field paused with the label `Cards[D54Adf7B-3148...][Field1]` instead of "Year of High School Graduation". Fixed with a generic sibling-label search; verified live end-to-end (a real work-authorization question later showed its correct label and was correctly left for the user, being high-risk).
    6. `_score_resume_parse_wait` false-positived on Ashby's "autofill from resume" widget's static placeholder copy, present before any file was ever selected - locked `detect_state` onto `resume_parse_wait` for an entire run on a fully-rendered, untouched form. Fixed: requires the file input to actually be gone.
    7. **Critical**: `_handle_resume_upload` searched for a button matching submit-or-next words after uploading - safe on a dedicated upload step, dangerous on a single-page form where the only matching button is the form's real, final "Submit Application" control. Would have submitted an application missing every other field, bypassing the approval gate entirely. Never actually fired live (bug #6 always intercepted first) but was one keyword-scoring tweak away from a real incomplete submission.
    8. `inspect_form` hard-required a semantic `<form>` element and raised otherwise - a real Ashby application page has zero `<form>` elements, 11 real fillable inputs sitting in plain `<div>`s. Fixed: falls back to the whole visible `<body>`.
    9. That `ValueError` (before fix #8 existed) propagated all the way out of `run_state_machine` and crashed the task to a hard `failed` status instead of the graceful `manual_intervention` pause every other failure mode gets. Fixed: `adapter.handle_state` calls are now wrapped, converting any exception into the same escalation path.
    10. Ashby keeps `<input type=file>` in the DOM even after a successful upload (adds a "Replace" affordance) - `resume_upload` kept re-winning every loop and `_score_application`'s `not has_file_input` gate never passed even once genuinely attached. Fixed with a `resume_already_attached` signal.
    11. `_handle_application_page` treated the absence of a next/continue control as a failure - wrong on a single-page form (nothing to advance to, only a final submit control). Fixed to only attempt navigation when a real next/continue button exists.
    12. `FieldMapper` had no rule for a single combined "name" field - Ashby's field is literally `_systemfield_name`. Fell through to the AI-agent path, which (`AI_PROVIDER=mock`) filled a real applicant's name with the mock gateway's canned placeholder text instead of "Brian Hsu". Fixed: a `full_name` rule synthesizing `first_name + last_name`, using this file's `[\s_-]` separator style rather than `\bname\b` (Python regex treats `_` as a word character, so `\b` never fires around it).
    13. Once #3/#10/#12 let `APPLICATION`/`SUBMIT_READY` both score 0.5 on a partially-filled single-page form, they tied every loop (`APPLICATION` wins ties, the safe side) - but nothing changed between iterations once every resolvable field kept resolving to the same value, so the run just re-filled the same fields forever until `MAX_TRANSITIONS` gave up. Fixed with `has_unfilled_visible_field`: `SUBMIT_READY` now scores 0 while a real empty field remains.
    14. `GenericAdapter` had no support for radio-button-group questions - a real "willing to relocate?" question rendered as two real `<input type=radio>` options sharing one `name`; `fill_field`'s only path for `type=radio` was the generic `page.fill()` fallback, which can't fill a radio input, so the question silently never got filled. Fixed: `inspect_form` now groups same-name radios into one field with `options` = each choice's own label text (mirroring `<select>`), and `fill_field` checks the matching one.
  - **Confirmed working correctly, not bugs**: CAPTCHA detection fired against a real Greenhouse reCAPTCHA challenge mid-run and correctly escalated rather than attempting to solve/bypass it - first live confirmation against a real vendor challenge, not just synthetic test snippets. The high-risk "never guess" rule correctly refused to auto-answer a real Lever work-authorization question, leaving it paused for the user.
  - **Known, real, understood limitations left open** (documented rather than rushed in the same session): Workday's Apply click producing no page change at all looks like server-side bot detection or an auth-gated flow, not a client-side bug - failing fast with a specific error is the right fix; going further would mean trying to defeat detection, which the spec prohibits. Ashby's city-autocomplete field is a real `<input>` with neither a `name` nor an `id` attribute, so `inspect_form` skips it outright - needs a deliberate design pass (likely keying off label/DOM-position), not a rushed addendum; this is the one remaining thing standing between the validation Ashby run and a real `submit_ready`. Real job postings sourced via `WebSearch` were frequently already closed/stale (3 of the first ~6 candidate URLs across Greenhouse/Workday) - not an automation bug (the state machine correctly detected `UNKNOWN` and escalated safely both times), but live-navigating a company's own careers-search page and picking a link directly from the results proved more reliable than trusting cached search results.
  - New tests (all reproduce real captured structure, several built directly from DOM snapshots pulled via the replay report, not hypothetical inputs): `test_generic_adapter_label_extraction.py`, `test_generic_adapter_button_text_matching.py`, `test_generic_adapter_landing_click_verification.py`, `test_generic_adapter_resume_upload_single_page.py`, `test_generic_adapter_formless_page.py`, `test_worker_handler_exception_escalation.py`, `test_generic_adapter_resume_already_attached.py`, `test_generic_adapter_submit_ready_needs_filled_field.py`, `test_generic_adapter_application_page_single_page_form.py`, `test_field_mapper_full_name.py`, `test_generic_adapter_partial_fill_no_deadlock.py`, `test_generic_adapter_radio_group.py`, `backend/tests/test_resumes.py`. 86/86 `apps/browser-worker/tests` pass, 98/98 backend tests pass as of this handoff.
- **Phase 5**: real Greenhouse, Lever, and Ashby ATS adapters, with capability reporting and fallback behavior when a site isn't supported.
- **Phase 6 - Discovery Automation**: scheduled search profiles, company watchlists, public ATS ingestion, change detection, and notifications.
- **Phase 7 - Tracking and Analytics**: a Kanban-style tracker, timeline view, follow-up reminders, email integration, outcome tracking, analytics, and search-strategy recommendations.
- **Phase 8 - Controlled Autopilot**: an automation policy engine, trusted-domain rules, daily limits, submission locks, a kill switch, and full audit enforcement.
  The spec explicitly says this phase must stay off by default and should be built deliberately last, after everything above it is solid.

## Known Issues / Bugs Already Fixed

These have already been diagnosed and fixed once.
Read this before touching the related code so they don't get reintroduced.

1. **bcrypt/passlib incompatibility**: `passlib`'s `CryptContext` broke against newer bcrypt releases.
   Fixed by calling the `bcrypt` library directly in `backend/app/core/security.py` instead of going through `passlib`.
2. **SQLAlchemy reserved-keyword collision**: columns literally named `metadata` on the `Profile`, `WorkflowTask`, and `ModelCall` models collided with SQLAlchemy's declarative `Base`.
   Renamed to `profile_metadata`, `task_metadata`, and `call_metadata` respectively.
   Any schema or serialization code must match these exact names.
3. **Python 3.9 compatibility**: code originally used the `X | None` union syntax (PEP 604), which is not valid on Python 3.9.
   It was rewritten to `Optional[X]` throughout the backend and browser-worker.
   The original dev server ran Python 3.9, so this was a hard constraint at the time.
   If running locally on a newer Python version this is no longer a hard constraint, but the codebase currently still uses `Optional[X]` style throughout, and new code should match that style for consistency.
4. **SearchProfile/JobMatchScore three-way drift** between the SQLAlchemy models, the Pydantic schemas, and the actual Postgres columns.
   Examples: the model had `is_active` while the DB had `enabled`, the model had `desired_titles` while the DB had `include_titles`, and dimensional scores were modeled as `0.0`-`1.0` floats when the DB and spec use `0`-`100` integers.
   This was fixed by aligning the models and schemas to the actual DB columns, confirmed via `psql \d search_profiles`.
   This class of bug, model/schema/migration drift, has now bitten this project twice.
   It is worth adding a CI check or test that asserts model columns match the actual DB schema before it happens a third time.
5. **Invalid Next.js config**: `next.config.js` had `output: standalone` as a bare identifier instead of `output: 'standalone'` as a string.
   Fixed.
6. **FastAPI DELETE endpoint bug**: DELETE endpoints returning a 204 status cannot declare a `-> None` response body annotation.
   This annotation was removed from the search-profiles delete route.
7. **Deploy target had only Podman, not Docker**: the original deploy target ("bhead" / hostname `kn-head`) only had Podman available, which forced the use of `podman-compose` everywhere.
   This is not relevant now that development runs on Docker Desktop locally, but `setup-portable.sh` still auto-detects `docker compose`, `podman-compose`, or `docker-compose`, in that priority order, in case this ever needs to run on another Podman-only host again.
8. **Auth was completely non-functional through the browser** (found and fixed 2026-07-17, while verifying Phase 3): NextAuth's login call POSTed JSON to the wrong internal URL when the backend actually requires a form-urlencoded `OAuth2PasswordRequestForm`; CORS didn't allow the actual web port (3002); `api-client.ts` never attached a bearer token to any request; the dashboard had no route guard; and the app had no `SessionProvider` at all, which NextAuth's client-side session management needs to behave correctly. All fixed together - see commit `47a9125`.
9. **Automated review findings crashed the application review page permanently**: the backend wrapped each finding as `{"message": str}` to satisfy an overly-strict Pydantic schema, but the frontend (correctly) typed these as `string[]` and rendered them directly, throwing "Objects are not valid as a React child." Since the page auto-fetches the stored review on mount, this broke the page on every subsequent load, not just the button click. Fixed by making the schema `List[str]` end to end - see commit `193a995`.
10. **`browser-worker` Docker image crashed on startup** (found and fixed 2026-07-20, while verifying it builds and runs): the base image `mcr.microsoft.com/playwright/python:v1.40.0-focal` ships Python 3.8, but `browser_worker/models.py` uses PEP 585 generic syntax (`list[str]`), which needs 3.9+. Fixed by switching to the `-jammy` variant of the same image (Ubuntu 22.04, Python 3.10).
11. **`browser-worker`'s Docker entrypoint had no real service loop**: `Dockerfile`'s `CMD` ran `python -m browser_worker.worker`, whose `if __name__ == "__main__"` block is a one-shot interactive demo (hardcoded fake application, headed browser, targets `localhost:8080`) meant for `run_demo.sh`/`QUICKSTART.md` on the host - not a Compose service. It crashed in the container (`Missing X server` - `HEADLESS` env var was never even read) and, more importantly, never claimed any real `WorkflowTask` rows. Fixed by adding `browser_worker/queue_worker.py` (the poll loop described above) and pointing `CMD` at it instead; `worker.py`'s demo entrypoint was left untouched for its original host-based use case.
12. **`browser-worker` was missing `pydantic-settings`**: importing `app.core.config` (needed transitively via `app.models`/`app.core.database`, which the poll loop reuses from the backend) failed with `ModuleNotFoundError`. Added `pydantic-settings==2.6.1` to `apps/browser-worker/requirements.txt`, matching the version pinned in `backend/requirements.txt`.
13. **`MockATSAdapter.navigate_next` resolved the wrong DOM element**: it used an unscoped `page.query_selector("button.next-btn")`, which Playwright resolves to the first matching element in DOM order - page 1's (hidden) button - rather than the button on whichever page is actually visible (each page has its own `.next-btn`). This broke navigation past page 2 every time (30s timeout, "element is not visible"). Fixed by scoping the query to the currently-visible `.form-page[data-page="N"]`, matching the pattern `inspect_form` already used. (`submit()` didn't need the same fix - there's only one `button[type="submit"]` in the whole page.) Also added detection for a click that "succeeds" but doesn't actually advance the page (client-side validation silently blocking it) - previously this caused `BrowserWorker._fill_application`'s navigation loop to retry forever with zero progress; now `navigate_next` returns `success=False` with the page's own validation error message when the page number doesn't change.
14. **`BrowserWorker.resume_from_checkpoint` couldn't actually resume a client-side multi-page form**: it navigates to `checkpoint.url` and, if the checkpoint step was `ready_to_submit`, immediately tries to click the submit button. But the mock ATS (and likely any similarly-built real ATS) implements its "pages" as show/hide `<div>`s on one URL, not real navigation - reloading the URL always resets to page 1, leaving the submit button (on page 3) hidden. Fixed by adding `BrowserWorker._replay_filled_fields`, which re-runs `inspect_form`/`fill_field`/`upload_document`/`navigate_next` page-by-page using `checkpoint.filled_fields` before attempting submit.
15. **`FieldMapper.get_value_for_field` crashed whenever `custom_fields` was `None`**: `ApplicationData.custom_fields` defaults to `None`, but the code did `if field.name in custom` without checking `custom` was truthy first, raising `TypeError: argument of type 'NoneType' is not iterable` on essentially every real call. Fixed with a truthiness check.
16. **`browser_checkpoints.form_state` was reaching Postgres completely unredacted** (found by an independent review pass, 2026-07-20): `CheckpointManager._redact_sensitive` was only ever applied to `filled_fields`; `form_state` (a raw DOM scrape of every visible input/select/textarea, excluding only `type=password`/`type=file`) went to storage as-is. A text-styled SSN or password-labeled-differently field would have been stored in plaintext. Fixed by redacting each nested form's fields the same way before either the Postgres or local-file write path.
17. **`queue_worker.py`'s poll loop could crash-loop the whole container after any DB write failure**: the `except Exception` handler that marks a task failed never called `db.rollback()` first, so if the exception itself came from a failed DB write, the session would be left rollback-pending and the subsequent `db.commit()` would raise `PendingRollbackError`, uncaught. Fixed by rolling back before writing the failure state. Not observed live (unreachable in the current single-process deployment), caught by review.
18. **AI-generated/human-typed answers for `<select>` fields could hang for 30s then fail** (found live, 2026-07-21): `field_resolution.py` was handing an agent's free-text answer (e.g. "Mock response generated successfully.") straight to `page.select_option()`, which only accepts one of the field's actual option strings - Playwright retried for 30 seconds before timing out. Fixed by `_constrain_to_options`, which maps free text onto real option values (exact match, substring, then a yes/no heuristic) and falls back to pausing with `paused_question` - never guesses, never hangs - when no confident match exists.
19. **Field-mapping cache hits weren't recorded, undercounting reuse**: `field_resolution.py`'s durable-mapping lookup (step 1) returned early on a cache hit without calling `record_mapping`, so `use_count`/`last_used_at` only ever reflected first-time regex learns, never actual reuse - misleading for the "reviewable" mappings list, which should show what's actually being relied on. Fixed by calling the (idempotent, upsert) `record_mapping` on the cache-hit path too; verified `use_count` incrementing correctly across 3 consecutive live runs.
20. **`field_mappings` migration failed with a duplicate-index error**: declaring `sa.Column(..., index=True)` and then also calling `op.create_index()` for the same column both create the same index under `op.create_table`, racing/duplicating. Fixed by dropping the redundant explicit `create_index` calls (`index=True` alone is sufficient, matching how every other indexed column in this migration set is already declared).
21. **`_constrain_to_options` missed answers using underscores/dashes where the option text uses spaces** (e.g. matching "I found it on a job board" against option value `job_board`): the substring check compared raw strings only. Fixed by also comparing an underscore/dash-normalized form on both sides; caught by the function's own unit test, not live traffic.
22. **`GenericAdapter`/`MockATSAdapter` had no defense against honeypot form fields** (found live against real Workday, 2026-07-21): Playwright's `:visible` pseudo-class is necessary but not sufficient - Workday's `data-automation-id="beecatcher"` field is `display:block; visibility:visible; opacity:1` with a `1px x 0.01px` box, passing `:visible` cleanly while being unusable by a human. Fixed by `browser_worker/services/field_visibility.py`'s `is_genuinely_fillable()`, a multi-signal check (size + type=file exemption + CAPTCHA-name exemption + label-discoverability) rather than a blunt size threshold, since real ATS platforms also ship *legitimate* near-zero-geometry fields (custom-styled selects, file inputs behind a styled button) that a naive size cutoff would have wrongly excluded too.
23. **No CAPTCHA detection existed anywhere in the state machine** (found live against real Greenhouse/Lever/Ashby, all of which use a real CAPTCHA vendor - reCAPTCHA or hCaptcha): `PauseReason.CAPTCHA` existed in `state.py` but nothing ever produced it. Fixed by `browser_worker/services/captcha_detection.py`, checked at the top of `worker.py`'s main loop ahead of `adapter.detect_state`. Deliberately does *not* trigger on the mere presence of a `g-recaptcha-response`/`h-captcha-response` field, since invisible reCAPTCHA v3 injects that on nearly every page load without ever presenting a challenge - only a visible interactive widget container/iframe triggers the pause.
24. **Test-fixture HTML snippets without an explicit CSS reset don't reproduce real near-zero-geometry field patterns**: a bare `<input style="width:0;height:0;">` with no border/padding reset actually renders at ~8x6px in Chromium (default UA border+padding on form controls), and a bare empty `<div class="g-recaptcha">` collapses to 0 height (no content to give it size) - neither matches how the real, live pattern actually measures (Workday's beecatcher genuinely measured 1x0.01px; a real reCAPTCHA widget has real rendered content once the vendor's script populates it). Both `test_field_visibility.py` and `test_captcha_detection.py` initially failed for this reason, not because of a bug in the implementation being tested - fixed by adding an explicit CSS reset (`border:none;padding:0;box-sizing:border-box;`) to the field-visibility test snippets and explicit realistic dimensions (e.g. reCAPTCHA v2's real ~304x78px checkbox) to the CAPTCHA-container test snippets.

## Known Remaining Gaps (not blocking, but worth knowing)

- The dashboard home page (`/dashboard`) still shows Phase 1's hardcoded mock stats and job cards - it was never wired to real data in any phase so far.
- There's no background worker to actually parse uploaded resumes or extract job postings - `parsed_data`/`extracted_data` stay empty after upload/import, so the tailoring pipeline has nothing real to work with until this exists. This blocks a fully realistic end-to-end walkthrough through the UI alone (verification required seeding `parsed_data` directly via SQL).
- No file-download/static-serving mechanism exists for rendered resume PDFs - `POST /api/resumes/{id}/render` writes a real file and returns its path, but there's no way to actually download it from the browser yet.
- No cover-letter generation exists - the review workspace shows a clearly-labeled placeholder instead.
- ~~`POST /api/resumes` doesn't persist the uploaded file~~ - **resolved 2026-07-21/22**: see the real-ATS-validation "What's Done" entry above (finding #1) - now writes bytes to the shared storage volume, filename sanitized since the path is a real filesystem write now.
- ~~`browser-worker`'s field mapping can't fill consent/attestation checkboxes~~ - **resolved 2026-07-20**: `fill_field` now handles checkboxes via `page.check()`/`uncheck()`, and `_handle_review` explicitly checks required attestation checkboxes (safe specifically because `submit_ready` still requires a separate human approve-submit click).
- ~~`browser-worker`'s orphan-reclaim only half-fixes the stuck-`running`-forever problem~~ - **resolved 2026-07-21**: `resume()` is now checkpoint-aware for every state (not just `approved_for_submit`), and `queue_worker.py` always calls `resume()`, so a reclaimed task correctly picks up from its last checkpoint instead of restarting. `backend/worker.py`'s equivalent for `job_extraction` still has no reclaim logic at all, though - a task stuck in `running` there is still orphaned forever.
- **No frontend UI exists for any of the manual-intervention/dynamic-question/field-mapping-review flows** - the backend endpoints (`resume-manual-intervention`, `answer-pending-question`, `GET/PATCH/DELETE /api/field-mappings`) are all built and verified via direct API calls, but nothing in `apps/web` surfaces a paused task, a pending question, or the learned-mappings review list yet.
- ~~Two browser-worker test files are flaky in combination, not in isolation~~ - **resolved 2026-07-21**: see the real-ATS-validation "What's Done" entry above - root cause was test emails not staying unique across separate `pytest` invocations against the long-lived mock-ats server, not anything about running the files together. Fixed in both files; three consecutive full-suite runs verified clean.
- ~~Real Workday validation has not been done~~ - **superseded 2026-07-21/22**: view-only research (2026-07-21) was followed by actual live runs with real applications (2026-07-21/22, see "What's Done" above) - 14 real bugs found and fixed, zero real submissions (every run stops at the approval gate or a genuine pause). Workday specifically never got past a "clicked Apply, nothing happened" wall that looks like server-side bot detection, not a fixable client bug.
- **Ashby's city-autocomplete field can't be filled yet**: it's a real `<input>` with neither a `name` nor an `id` attribute, so `inspect_form`'s `if not name and not input_id: continue` skips it outright. Needs a deliberate design pass (likely keying off label/DOM-position instead of name/id attributes) - this is the one thing currently standing between a real Ashby validation run and an actual `submit_ready`.
- **Workday's real "Apply" click is a no-op** on at least one live-tested posting (U.S. Bank) - a real, visible link with a valid `href`, but clicking it produces no navigation and no content change even after several seconds, and a direct navigation to the resulting `/apply`-suffixed URL never reaches `networkidle` within 30s either. Looks like server-side bot detection or an auth-gated flow; the spec's "no anti-bot circumvention" principle means the correct fix is exactly what exists now (fail fast with a specific, honest error), not trying to work around it.
- Minor: `GET /api/resumes/{id}` (singular, by id) doesn't exist on the backend - the resume detail page calls it once for family metadata and silently no-ops on the 404; harmless but should either get a real endpoint or have the dead call removed.
- Minor: blank high-risk answers awaiting user input are labeled "Your edit" in the question list, which reads as if the user already typed something - worth a distinct label for "needs your input" vs. "you edited this."
- Minor: the "View Resume" link on the application review page uses the resume's `family_id` instead of its `id`, so it currently links to a URL the resume detail page can't resolve.

## Current Local Environment Status (as of this handoff)

Verified locally on 2026-07-17, running on Docker Desktop (not the old remote server). All six services are up: `postgres`, `redis`, `minio`, `api`, `mock-ats`, and `web`.

- The API is confirmed healthy: `GET /health` returns 200, and `/docs` serves the Swagger UI correctly.
- The database was one migration behind (`002_add_model_calls` instead of head).
  It has been brought up to date; head is now `e9374f596c0a` (`add_search_profiles_and_matching`).
- `apps/web/.env.local`, `apps/web/.env.example`, and `README.md` had stale references to the API running on port 8000.
  These have been fixed to point to the correct port, 8001.
- The `web` frontend now builds and runs.
  Getting there required fixing several real bugs, not just the original lint errors:
  - `react/no-unescaped-entities` lint errors (raw apostrophes in JSX) in `app/auth/login/page.tsx` and `app/dashboard/applications/[id]/page.tsx`, fixed by escaping them.
  - A genuine data-model gap: `GET /api/resumes` only returns resume *families*, but the job-detail page needs resume *versions* to match against `selection.selected_resume_id` (which is a version id, confirmed via `ResumeSelectionAgent`). Added a new `GET /api/resumes/versions` endpoint and a matching `apiClient.getResumeVersions()` method, and pointed the job-detail page at it instead of silently type-casting the wrong shape.
  - Missing NextAuth type augmentation (`session.user.id` had no type), fixed by adding `apps/web/types/next-auth.d.ts`.
  - A missing `apps/web/public/` directory that the Dockerfile expects to copy.
  - `docker-compose.yml`'s `web` service bind-mounted `./apps/web:/app` over the built image, which overwrote the standalone-build `server.js` with raw source and crash-looped the container. Removed that bind mount and its companions (`/app/node_modules`, `/app/.next`) since the Dockerfile builds a self-contained production `standalone` bundle, not a dev server - live-reload was never wired up for this service, so there is currently no hot-reload frontend dev loop in Docker. To iterate on frontend code with hot reload, run `cd apps/web && npm install && npm run dev` directly on the host instead of through Compose, pointed at the dockerized API on port 8001; re-run `docker compose build web` only when you want to verify the production build.
  - Host port 3000 was already taken by an unrelated container from a different project (`media-converter-frontend-1`). Remapped `web` to host port **3002** instead of touching that container; `NEXTAUTH_URL` was updated to match.
- `browser-worker` is now a real persistent service (see the Phase 4 entry in "What's Done" above), built and running, verified end-to-end live against `mock-ats` up through the assisted-mode pause/approve/resume/replay steps.
- Running `./phase2_test.sh` against the live stack passed steps 1 through 6 (create user, update profile, create search profile, import job, calculate match score, retrieve match score) but failed at step 7, resume upload.
  The script expects a top-level `id` in the response, but the actual `/api/resumes` POST response returns a nested `{"family": {...}, "version": {...}}` shape, each with its own `id`.
  This looks like the API's resume-family/resume-version split shipped after the test script was last updated; the script itself is stale, not necessarily the API.
  The script is also not idempotent: rerunning it with the same hardcoded test email fails at step 1 since that user already exists.
  This script should be updated as part of Phase 3 work, since Phase 3 will add more resume/answer endpoints that deserve smoke-test coverage anyway.

## Local Setup Instructions

This all assumes Docker Desktop running locally, not the old remote dev server.

1. From the repo root, run `docker compose up -d`.
   This brings up `postgres`, `redis`, `minio`, `api`, `web`, `mock-ats`, `job-worker`, and `browser-worker`.
2. `browser-worker` runs continuously as of 2026-07-20 (see Phase 4 above) - it's a regular Compose service now, not a profile, so it starts automatically and polls for `browser_submission` `WorkflowTask` rows on its own.
3. Service endpoints:
   - API: `http://localhost:8001` (not 8000; port 8000 was remapped to avoid a conflict left over from the old remote server context, kept as-is for consistency with existing docs and scripts).
   - Frontend: `http://localhost:3002` (not 3000; remapped to avoid a conflict with an unrelated container from another project on this machine - if that conflict doesn't exist on your machine, feel free to change it back to 3000 in `docker-compose.yml`).
   - Mock ATS: `http://localhost:8080`.
   - MinIO console: `http://localhost:9011`.
   - Postgres: `localhost:5432`.
   - Redis: `localhost:6379`.
4. `AI_PROVIDER=mock` is the default in `backend/.env`.
   Set it to `anthropic` or `openai` along with a real API key to exercise non-mock behavior; this path has not been tested yet.
5. Test credentials from prior E2E runs: `demo@test.com` / `demo123`.
   These are ephemeral, dev-only credentials, not a production account.
6. Run `./phase2_test.sh` for a smoke test of the combined Phase 1 + Phase 2 API flow.

## Deployment Notes

Previous work happened on a remote dev server reached via `ssh bhead` (hostname `kn-head`).
That was not this session's actual target and should not be treated as the deployment target going forward.

The real deployment target going forward is a VPS called "hermes."
This was not mentioned anywhere in `spec.md` or the prior session transcript, so no existing infrastructure or configuration in this repo assumes it.
Deployment tooling and any hermes-specific configuration still need to be created from scratch in a future phase.

## Where to Find Things

- `spec.md` - the full product specification, including the phase breakdown in section 27.
- `session_transcript.txt` - a detailed history of the Phase 1 and Phase 2 build session.
- `PHASE2_TEST_REPORT.txt` - detail on the Phase 2 test run referenced above.
